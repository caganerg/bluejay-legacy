# Run every script with the Bun runtime instead of Node.
# Without this, the shebangs under node_modules/.bin (#!/usr/bin/env node)
# would look for Node on the system — and Node is not installed on this machine.
[run]
bun = true
